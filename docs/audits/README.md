# Audits, incidents and reports - what happened once, on a date

Crash reports, dumps, defect post-mortems, upstream syncs, session handoffs. **Each is a snapshot
and none is maintained.** Read the date first; the code has moved.

They are kept rather than deleted because several are cited from source comments as the reason a
piece of code looks the way it does - `GAMUT_MATRIX_DEFECT_2026-08-12.md` is referenced from the
mixer for exactly that, and deleting it would leave a comment pointing at nothing.

**Known stale link:** `SESSION_HANDOFF_2026-08-16.md` links to `OCIO_HANDOFF_2026-08-11.md`, which
was deleted in `ea7a96c2b` on 2026-08-16. Broken since then, not by the reorganisation - recorded
here rather than quietly repaired, because the handoff's reasoning refers to content that no
longer exists anywhere.
